# 제품 포장 단계 전환 성능 패치

## 원인
제품 포장 화면 진입 시 선택된 각 제품마다 모든 보유 박스를 대상으로 `loadContainer()` 기반 정밀 적재 시뮬레이션을 반복 실행하고, 자동설계 박스 후보까지 다시 시뮬레이션하고 있었습니다. 제품 수와 박스 종류 수가 늘면 동기 계산량이 곱셈으로 증가해 다음 단계 버튼을 누른 뒤 화면이 오래 멈췄습니다.

## 변경
- 제품 포장 단계에서는 치수, 중량, 내부 적층 한도, 박스당 최대수량, 차량 치수 효율만 빠르게 계산합니다.
- 보유 박스가 맞으면 보유 박스 후보만 우선 사용합니다.
- 맞는 보유 박스가 없을 때만 자동설계 박스를 계산합니다.
- 동일 입력 후보는 메모리 캐시를 사용합니다.
- 실제 차량 내부 배치, 충돌, 지지, 무게중심, 물리 검증은 다음 자동 적재 단계에서 계속 수행합니다.

이 패치는 안전 검증을 제거하는 것이 아니라, 포장 후보를 고르는 화면에서 중복 수행되던 차량 전체 시뮬레이션만 제거합니다.
