# Changelog

## v2.4.0 — UX 정합성 및 운송 시나리오 보완

- 대시보드에서 실제로 동작하지 않던 단일 옵션 select와 무연결 회전/혼합 체크박스를 제거하고 고정 동작을 설명형 UI로 변경
- 상단 `대시보드 / 3D 보기 / 결과 보기`를 React `onClick`에 직접 연결하고 활성 상태를 React state로 일원화
- `DashboardRuntimeEnhancer`의 버튼 문구 검색, `classList.toggle`, `dataset.viewMode` 기반 그림자 네비게이션 제거
- 상태 메시지를 `info / success / warning / error`로 구분하고 `role=status/alert`, `aria-live` 적용
- 처음 방문 시 실제 데이터처럼 보이던 기본 샘플 자동 로드를 제거하고 빈 상태 + 3단계 온보딩 + 랜덤 샘플 CTA 제공
- 결과 고급 패널 8개 동시 노출을 `비교/최적화 · 수동 편집 · 작업 준비` 3단계 탭으로 분리
- 레이아웃 보정 CSS의 `!important` 의존 제거 및 상태/온보딩/고정설정 스타일 정리
- 유니코드 장식 아이콘 의존을 줄이고 주요 버튼을 명확한 텍스트 라벨로 정리
- 수량 증감 버튼에 접근성 라벨 추가, 결과 모달 Escape 닫기 지원
- README / package.json / CHANGELOG 버전을 v2.4.0으로 동기화
- Rapier 운송 종합검증에 `출발 가속 0.30g` 시나리오를 추가해 후방 관성 방향 이동/전도 검사
- 박스/팔레트 4면 정보 스티커의 카메라 각도별 z-fighting 완화
- 팔레트 저활용률 배치를 폭 방향 중앙 정렬 레인 방식으로 보완

## v2.3.0 — 분석보고서 기반 팔레트 전역 최적화

- 팔레트 자동 적재에서 박스 층수 최대 후보와 2~3단 팔레트 적층 후보를 함께 생성해 전역 비교
- 후보 선택 기준을 적재수량 보존 → 바닥 점유 열 최소 → 팔레트 수 최소 → 좌우 중량 편차 최소 순으로 정리
- 체적 활용률 50% 미만인 팔레트 적재안은 컨테이너 길이 전체를 활용하는 균등 재배치 후보 적용
- 균등 재배치 시 팔레트 폭/길이를 고려해 물리적으로 겹치지 않는 슬롯만 사용
- 전체 팔레트 쌍을 반복 탐색해 더 이상 합칠 수 없을 때까지 병합하는 반복 consolidation 추가
- 팔레트 화면에 선택된 적층 후보, 바닥 점유 열, 균등 재배치 여부, 추가 병합 횟수 표시
- 미검증 작업지시서와 Excel 내보내기에 확인 절차 추가 및 작업지시서에 `물리 안정성 미검증` 워터마크 표시
- 제약 지표 `문쪽 개방 여유`를 실제 의미에 맞게 `문쪽 낙하 위험`으로 변경
- `문쪽 공간 활용도` 지표를 별도로 추가해 길이 활용률과 문쪽 공백 mm 표시
- 바닥 하중 경고 기준을 컨테이너별 `kg/m²` 한계와 평균 대비 경고배수로 설정 가능하게 변경
- 화물/모드/컨테이너 변경 시 이전 물리검증 결과를 즉시 무효화해 오래된 검증으로 내보내기가 허용되지 않도록 보완
- 최대중량 바로 아래에서 다음 박스가 한도를 초과하는 경우 미적재 사유를 정확히 `최대 적재 중량 초과`로 기록
- 물리 우선 안전 구조와 충돌하던 과거 중앙 낱개/돌출 타워 고정 임계치 회귀 테스트를 진단 지표 테스트로 정리
- 회전 회귀 테스트를 `회전하지 않으면 물리적으로 들어가지 않는` 조건으로 강화
- 팔레트 전역 최적화·문쪽 지표·바닥하중 설정 신규 단위 테스트 추가

## v2.2.0 — Rapier 물리 안정성 검증 완성

- `@dimforge/rapier3d-deterministic-compat` 기반 강체 물리 검증 엔진 추가
- 기존 정적 좌표 검증과 별개로 중력·충돌·마찰을 실제 물리 월드에서 재검증
- 정적 중력, 급제동 0.5g, 횡가속 0.35g 운송 시나리오 종합검증 추가
- 박스별 수평 이동량, 높이 변화, 기울기, 선속도, 각속도, 경계 이탈 판정 추가
- 안정 / 주의 / 불안정 분류와 100점 물리 안정성 점수 추가
- 박스 모드와 팔레트 모드의 공용 물리 검증 타깃 브리지 추가
- 팔레트 바닥판을 자체 중량을 가진 동적 강체로 모델링해 2~3단 팔레트 지지/이동 위험 검증
- 팔레트 자체 안정 / 주의 / 불안정 수량과 위험 위치 표시 추가
- 물리 위험 박스를 기존 3D 선택 시스템과 연결
- 물리 검증 결과 이벤트와 최신 결과 전역 캐시 추가
- 물리 검증 모듈을 dynamic import로 분리해 초기 대시보드 번들 영향 최소화
- 대량 적재 시 step 수 자동 조절 및 주기적 UI yield로 브라우저 멈춤 완화
- PC/모바일 대응 물리 검증 모달과 시나리오별 점수 카드 추가
- 인쇄/PDF 저장용 물리 안정성 리포트 추가
- 물리 리포트 사용자 문자열 HTML escape 처리
- 정적 지지, 비지지 낙하, 운송 시나리오, 팔레트 지지체 단위 테스트 추가
- Playwright에 BOX/PALLET 물리 검증 및 모바일 버튼 노출 E2E 추가

## v2.1.0-rc — 운영형 시뮬레이터 확장

- 최대 적재율형 / 안정성 우선형 / 하역 편의형 3개 전략 비교 및 공통 점수 평가 추가
- 하역 우선순위 필드 추가 및 전략·Excel 입출력에 연결
- 다중 컨테이너 자동 분할 계획 추가
- 빈 공간 추가 적재 추천: 품목별 추가 가능 수량, CBM, 중량, 예상 적재율, 추천 구역 계산
- 안전 수동 편집기 추가: X/Y/Z 5cm 이동, 90도 회전, 실시간 경계·충돌·지지·적층·상부하중 검증
- 3D 개별 박스 직접 드래그, 고스트 미리보기, 안전 상태 표시, 스마트 스냅 추가
- 같은 품목 / 같은 행 / 같은 층 묶음 선택과 블록 이동 추가
- 블록 이동 안전 후보 TOP 3 추천 및 블록 전체 3D 직접 드래그 추가
- 그룹 드래그 실행취소 및 자동 배치 복귀 흐름 연결
- 작업 순서 엔진 추가: 적재 시 하부 지지 박스 우선, 하역 시 상부 박스 우선, 하역 우선순위 반영
- 작업 순서 재생/일시정지/속도 조절/이전·다음/3D 자동 선택 UI 추가
- 현장 단계별 완료 체크와 적재·하역 진행률 세션 저장 추가
- 작업자 취급 위험 점검 추가
- Excel 결과에 적재작업순서 / 하역작업순서 시트 추가

## v2.0.0-rc — 개발 완료 후보

- 3열 운영 대시보드로 전면 개편
- 바닥 하중 12×4 격자와 제약조건 분석 모듈 추가
- Excel 결과 내보내기 및 작업지시서 강화
- 모바일 대시보드 대응

## v1.5.0

- 3D 박스 클릭 선택 및 작업지시서 생성
- `MANUAL_QA.md`, `SEPTEMBER_CI.md` 추가

## v1.4.0

- Playwright 데스크톱/모바일 smoke test 구성
- 2026년 8월 Actions Billing 제한으로 자동 실행 일시 중지

## v1.3.0

- 전역 Error Boundary, Vercel 설정, 초기화 확인 절차 추가

## v1.2.0

- InstancedMesh 대량 렌더링, Excel 즉시 반영, 모바일 개선

## v1.1.0

- 3D 뷰어 lazy loading 및 초기 번들 축소

## v1.0.0

- 박스 단독/혼합 적재, 팔레트 적재, 회전, 중량/적층/상부하중 제약, 3D 시각화, 엑셀/로컬 저장
