# Codex 개선본 · 2026-09-21

원본 ZIP의 current_source를 복사하여 수정했습니다. 원본 ZIP은 변경하지 않았습니다.

## 실행

이 폴더에서 `pnpm install`, `pnpm dev --host 127.0.0.1 --port 4173`을 실행하고 http://127.0.0.1:4173 을 여세요. npm 사용 시 `npm install`, `npm run dev -- --host 127.0.0.1 --port 4173`도 가능합니다. Node.js 22 이상 권장.

## 변경

- 장비 선택 화면에 실제 내부 규격 기반 Three.js 캔버스 추가: 회전, 확대, 내부 공간/장비 모델 전환.
- 자동 적재 화면에 외벽 전환과 높이 단면 필터 추가. 단면은 선택 높이 아래에서 시작하는 박스를 표시하며 실제 적재 좌표를 바꾸지 않습니다.
- 체적 사용률, 잔여 체적, 적재 중량, 규칙 검사 결과 표시.
- 인스턴스 박스의 바운딩 구 갱신으로 장면 변경 시 잘못된 화면 밖 판정 방지.
- 최종 후보 재검사: 경계, 충돌, 등록 규격과 회전, 수량, 지지 면적, 혼합 SKU 적층단, 누적 상부 하중, 총중량.
- 안전한 후보와 무효 후보 점수 비교 오류 수정. 대량 화물 고속 경로도 검사를 수행. 두 후보 모두 무효면 미적재 사유를 반환.
- 저장된 수동 배치 복원 시에도 적재 규칙 검사.
- Windows 번들 검사 경로 수정. pnpm-lock.yaml 추가.

## 적용 범위

자동 적재 후보 선택 개선은 DIRECT BOX 엔진에 적용했습니다. 팔레트 최적화 알고리즘과 기존 관성/물리 검증 흐름은 유지했습니다.
지지 최소 비율은 기존 65%입니다. 누적 상부 하중은 기존 보수적 전하중 전달 기준으로 검사합니다. 중심 편중과 바닥하중 분포는 기존 평가·경고 체계를 사용합니다.
이번 작업은 새 CAD 파일 업로드나 임의 형상 충돌 검사가 아니라, 장비/직육면체 화물 규격에 따른 3D 공간 모델링입니다.

## 검증 결과

- 기존 단위·회귀 테스트 377개 통과, 신규 안전 규칙 테스트 9개 통과.
- 기존 데스크톱/모바일 스모크 10개 통과.
- 새 3D 조작 + 진행 중 단면 변경 + 최종 물리 검사 완료 E2E 2개 통과.
- 타입 검사, 아키텍처 검사, 프로덕션 빌드, 번들 용량 검사 통과.
- 후속 변경에 영향을 받는 적재 엔진 테스트 20개 재검증 통과.
- 브라우저 화면 캡처를 확인하고 모델 로딩 및 모바일 도구막대 레이아웃을 수정했습니다.

확인된 호환성 수정: 화면 전용 입력(data-view-only)은 관성 인증을 무효화하지 않습니다. 치수 표시 글꼴 로딩은 독립 Suspense로 분리해 3D 모델 렌더링을 막지 않습니다.
