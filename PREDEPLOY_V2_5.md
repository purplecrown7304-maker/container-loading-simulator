# v2.5.0 Pre-deploy Gate

이 문서는 `v2.5.0`을 Production에 반영하기 전에 반드시 통과해야 하는 최종 게이트입니다.

> **원칙:** 아래 자동 검증과 필수 수동 시나리오 중 하나라도 실패하면 `main` 병합 및 Production 배포를 진행하지 않습니다.

## 1. 자동 검증

Node 22 환경에서 저장소 루트 기준으로 실행합니다.

```bash
npm install
npm audit --audit-level=high
npm run verify:predeploy
npx playwright install chromium
npm run test:e2e
```

`npm run verify:predeploy`에는 다음이 포함됩니다.

1. Vitest 전체 회귀 테스트
2. TypeScript strict 검사 + Vite production build
3. 초기 번들 크기 예산 검사

브라우저까지 포함해 한 번에 실행할 수 있는 환경에서는 다음 명령을 사용할 수 있습니다.

```bash
npm run verify:full
```

## 2. 최종 관성 인증 게이트

### DIRECT BOX
- [ ] 자동 적재 후 최종 결과가 바로 열리지 않고 관성 인증이 먼저 실행된다.
- [ ] 출발 가속 / 급정거 / 급회전 3종을 모두 통과해야 결과가 열린다.
- [ ] 실패 시 미끄럼방지재 → 블로킹재 → 고정바가 필요한 단계까지만 자동 추가된다.
- [ ] 현재 적재 좌표가 바뀌면 이전 PASS가 즉시 무효화된다.
- [ ] 최대 이동과 기울기 기준을 넘으면 결과가 잠긴다.

### PALLET
- [ ] 팔레트 적재안도 최종 결과 전에 동일하게 관성 인증을 거친다.
- [ ] 최대 전체 이동 ≤ 12 mm, 최대 기울기 ≤ 1.8°를 만족해야 한다.
- [ ] 화물↔팔레트 상대 미끄럼 ≤ 8 mm를 만족해야 한다.
- [ ] 바닥 팔레트↔컨테이너 또는 상단 팔레트↔하단 팔레트 상대 이동 ≤ 12 mm를 만족해야 한다.
- [ ] 밴딩/랩핑은 화물을 자기 팔레트에 구속하고, 상단 팔레트를 컨테이너 바닥에 직접 묶지 않는다.
- [ ] 고정바 구속력은 바닥 팔레트에만 적용된다.
- [ ] 결과 화면에 화물 상대 미끄럼, 팔레트 상대 이동, 내부 최대 구속력이 표시된다.

## 3. 팔레트 적재 회귀

- [ ] 품목 처리 우선순위가 `중량 내림차순 → CBM 내림차순 → 코드순`으로 결정된다.
- [ ] 1차 적재에서는 같은 품목끼리 팔레트를 채운다.
- [ ] 혼합 적재는 모든 순수 품목 적재 후 팔레트 수를 실제로 줄일 수 있을 때만 최종 병합으로 실행된다.
- [ ] `maxTopLoadKg` 미입력은 팔레트 적층 자체를 차단하지 않는다.
- [ ] `maxTopLoadKg`가 입력되어 있고 상부 하중이 이를 초과하면 적층이 차단된다.
- [ ] 팔레트 최대 적층단 1~3 설정을 넘지 않는다.
- [ ] 팔레트 면적 지지가 부족한 상부 팔레트는 적층되지 않는다.
- [ ] 컨테이너에 실제 팔레트 슬롯이 1개뿐인데 2개가 필요한 경우, 2번째 팔레트가 원점에 겹치지 않고 화물이 `미적재`로 반환된다.
- [ ] 팔레트 자체가 컨테이너 폭/길이보다 큰 경우 팔레트 0개, 배치 0개, 최대 적층단 0으로 반환된다.
- [ ] 활성화된 각대/랩핑의 가능한 중량까지 포함해 컨테이너 최대중량을 초과하지 않는다.

## 4. 보조자재 / 3D 시각화

- [ ] 팔레트 PASS 결과의 3D에서 밴딩, 각대, 랩핑, 미끄럼방지재, 고정바가 실제 사용량에 맞게 표시된다.
- [ ] 직접적재 PASS 결과에서 미끄럼방지재, 블로킹재, 고정바가 표시된다.
- [ ] 보조자재 단위중량 설정을 변경하면 기존 관성 인증 서명이 달라져 재검증된다.
- [ ] 화면의 보조자재 총량과 작업지시서/Excel 총량이 일치한다.
- [ ] 팔레트별 보강재 합 + 공통 고정바 중량 = 최종 추가 보조재 중량이다.

## 5. 작업지시서

### 박스 직접적재
- [ ] 위에서 본 적재도 / 옆에서 본 적재도 / 3단계 진행 그림이 표시된다.
- [ ] 동일 작업 묶음은 개별 박스 수십 줄이 아니라 그룹 번호와 수량으로 표시된다.
- [ ] `안쪽부터 → 바닥부터 → 번호 순서대로` 지시가 명확하다.
- [ ] 관성 인증이 현재 적재안과 일치하지 않으면 출력되지 않는다.

### 팔레트
- [ ] 위/옆 그림의 P번호, C번호, 단수가 작업표와 동일하다.
- [ ] 각 팔레트는 `미끄럼방지 → 배치 → 각대 → 밴딩 → 랩핑 → 확인` 순서로 표시된다.
- [ ] 고정바는 모든 팔레트 배치 후 공통 최종 작업으로 분리된다.
- [ ] 관성 안전지표와 자동 보강 이력이 현재 인증 결과와 일치한다.

## 6. Excel

- [ ] DIRECT BOX / PALLET 모드에 맞는 파일이 자동 생성된다.
- [ ] 최신 관성 PASS가 없거나 적재 좌표가 바뀐 경우 내보내기가 차단된다.
- [ ] `보조자재` 시트가 있다.
- [ ] `관성보강이력` 시트가 있다.
- [ ] `관성안전지표` 시트가 있다.
- [ ] 팔레트 모드에 `팔레트별결속` 시트가 있다.
- [ ] 팔레트별 밴딩/각대/랩핑/미끄럼방지 수량이 작업지시서와 동일하다.

## 7. UI / 모바일

- [ ] 1366px 이상 데스크톱에서 주요 패널과 3D가 겹치지 않는다.
- [ ] 390×844 모바일에서 가로 스크롤이 발생하지 않는다.
- [ ] 박스/팔레트 모드 전환 후 3D가 정상 갱신된다.
- [ ] 관성 검증 중 진행률과 현재 시나리오가 표시된다.
- [ ] 실패 상태에서 결과 모달이 우회해 열리지 않는다.

## 8. Production 직전 조건

다음 조건이 모두 충족될 때만 `main` 병합/배포합니다.

- [ ] `npm audit --audit-level=high` 성공
- [ ] `npm run verify:predeploy` 성공
- [ ] `npm run test:e2e` 성공
- [ ] 위 필수 수동 시나리오 통과
- [ ] PR이 최신 `main`과 mergeable 상태
- [ ] 미해결 blocking review/thread 없음
- [ ] Vercel build-rate-limit 해소 또는 정상 Production 빌드가 가능한 상태
- [ ] 직전 정상 Production deployment/commit을 롤백 기준으로 기록

## 9. 현재 외부 제한

2026년 8월 GitHub Actions는 Billing 제한 때문에 `workflow_dispatch` 수동 실행 전용입니다. Vercel Preview/Production도 계정의 `build-rate-limit` 상태가 발생할 수 있습니다. 이 외부 제한은 코드 오류와 구분해서 기록하되, **실제 자동 검증 성공 기록 없이 Production 배포를 승인하지 않습니다.**

## 10. 안전 고지

관성 PASS와 보조재 구속력은 시뮬레이터 내부 비교 모델입니다. 실제 운송 안전 인증이나 밴딩/고정바 제조사 정격을 대체하지 않습니다. 실제 작업 전 박스 압축강도, 팔레트 상태, 결속재 규격/정격, 컨테이너 바닥/차량 조건 및 현장 안전기준을 확인해야 합니다.
